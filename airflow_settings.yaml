# Local dev only — Airflow variables (voir doc Astro)
# https://www.astronomer.io/docs/astro/cli/develop-project#configure-airflow_settingsyaml-local-development-only

airflow:
  # Used by inference_pipeline_dag — JSON array of YYYY-MM-DD strings.
  variables:
    - variable_name: INGESTION_DATES
      variable_value: '["2023-12-22", "2023-12-23"]'
